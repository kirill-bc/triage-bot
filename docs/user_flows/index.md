# User flows index

Map end-to-end tests (`pytest -m e2e`) to documented flows here as scenarios are added.

| Flow id | Description | E2E coverage |
|---------|---------------|--------------|
| _TBD_   | Support-created bug triage (mismatch / match) | Placeholder skip in `tests/e2e/` |
