# User flows index

Document support triage **flows** here (identifiers, steps, expected outcomes). Use them when
writing **integration** tests (`pytest -m integration`) or manual validation in a test Jira
project. This repo does not use browser E2E for the triage service (Lambda-oriented).

| Flow id | Description | Automated coverage |
|---------|-------------|----------------------|
| _TBD_   | Support-created bug triage (mismatch / match) | Planned integration tests (mocked Jira) |
